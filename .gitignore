# Python virtual environment (recreate with: python -m venv .venv + pip install -r requirements.txt)
.venv/
__pycache__/
*.pyc

# Local vector database (regenerated automatically on first use)
.chroma/

# Secrets — never commit API keys
.env
.streamlit/secrets.toml

# Personal test documents (copyrighted / private material stays local)
iso27001.pdf
